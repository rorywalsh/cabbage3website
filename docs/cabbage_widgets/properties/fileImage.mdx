```json
"file": "filename"
```
This property sets the file to be displayed by the image widget. The following image formats should be supported on most systems: PNG, JPEG, GIF
BMP, SVG (Scalable Vector Graphics). 

>Note: SVG files might not load if they contains scripts or other features that violate security policies (like CORS - Cross-Origin Resource Sharing).