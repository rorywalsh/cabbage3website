```json
"text": "string"
```
or
```json
"text":{ "on":"string", "off":"string" }
```
"string" will be the text that appears on the widget. `button` widgets take two strings, one for on and one for off.
