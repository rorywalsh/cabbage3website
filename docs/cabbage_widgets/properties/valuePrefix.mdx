```json
"valuePrefix": "valuePrefix"
```
Sets the prefix that is prepended to parameter values when displayed in the host and the plugin GUI. Defaults to "". `xypad` widgets take two strings, one of the x values, and one for the y values.
